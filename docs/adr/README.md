# Architecture / programme decision records

One file per decision node (`DEC-*`, `GE03`) or consequential choice: options,
tradeoffs, decision, consequences, and the recorded human approval (link to
the GitHub issue comment). Created when a decision node is executed — a
`humanGate` node is not `done` until its ADR exists and approval is recorded.
