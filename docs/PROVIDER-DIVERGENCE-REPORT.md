# Provider divergence report (generated)

Generated by `python3 tools/provider_divergence.py` — do not hand-edit.
Measures cross-provider behavioural divergence for the reference
matchup on the deterministic engine (T-028). The provider table is
**simulated fixture data** (`fixtures/providers.yaml`); no live
provider is called — live cross-provider execution is a separate,
approval-gated lane.

- Declared order (defender): `['ollama', 'anthropic', 'openai']`
- Selection against the fixture table: `ollama` (status **degraded**, missing: ['contextWindow'])
- Providers compared: `['anthropic', 'ollama']`
- Seeds run: 50
- Trace divergence rate: **1.00** (any byte of the replayable evidence differs — the effective fuel scale is part of the trace)
- Outcome divergence rate: **0.00** (the winner changes between providers)

## Divergence channel

declared numeric requirements capped by provider capabilities (effective fuel); live provider execution is a separate approval-gated lane.

## Outcome-divergent seeds

None at the current reference configuration: matches resolve before the fuel channel can flip an outcome. The tie-break path (turn-limit matches decided on absolute fuel) is where provider caps flip winners; the suite exercises it directly (T-028).
