# REASONS-LITE — Reddi Arena Project Kickoff

_Status: done | Owner: Claude Code session on behalf of Nissan | Project: reddi-arena | Issue/PR: seed commits on main (pre-issue-graph)_

## R — Requirements / Definition of Done

- Create a separate repo from RAP and the lab for the Arena proof use case.
- Use GitHub issues as the planning/tracking spine, generated from the
  dependency graph in `plan/backlog.yaml`.
- Bind every issue to a REASONS-LITE artifact in `spdd/prompt/` (generated by
  `tools/generate_prompts.py`; the graph decides topological order).
- Deploy the playable preview to Railway from this repo.

DoD checklist:

- [x] Local project scaffold exists (core, cli, web, adl, tools, tests, docs).
- [x] 55-test suite green; balance report published; landing page honest.
- [x] SPDD artifacts + issue script generated from the plan graph.
- [x] GitHub repo `nissan/reddi-arena` exists and is pushed.
- [x] `github/create-issues.sh` run once; issue numbers recorded (#1–#36 via
      GitHub API — environment had no `gh` CLI; same titles/bodies/labels/order).
- [x] Railway service created from the repo; domain generated; preview verified.

## E — Entities / Handoff Objects

| Entity / object | Purpose | Key states/fields | Existing or new? |
|---|---|---|---|
| plan/backlog.yaml | Canonical dependency graph | phases, lanes, epics, issues, tests; DAG-linted | Existing |
| spdd/prompt/*.md | Per-issue binding artifacts | REASONS sections + Claude Code prompt + sync log | Generated |
| github/create-issues.sh | Issue graph creation | labels lane/phase/epic/status; dep cross-refs | Generated |
| CLAUDE.md | Claude Code repo prompt | methodology, commands, hard boundaries | New |
| Railway service | Public preview | Dockerfile build, /api/health check, optional volume | New |

## A — Approach / Key Decisions

- Generate prompts and issues FROM the graph rather than writing them by hand:
  the plan linter already guarantees the DAG, so topological order is free and
  artifacts cannot drift from the plan without regeneration showing it.
- Follow the lab's SPDD/Loop Protocol verbatim rather than inventing a variant.
- Rejected: folding Arena into reddi-agent-protocol (blurs proof with product)
  or reddiagent-lab (violates one-way canonicality).

## S — Structure / Files Touched

| Surface | Expected change | Owner |
|---|---|---|
| CLAUDE.md | Claude Code guidance | this kickoff |
| spdd/prompt/, spdd/INDEX.md | 36 generated artifacts + index | generator |
| github/create-issues.sh | issue graph script | generator |
| Dockerfile, railway.json | deploy config | this kickoff |

## O — Operations / Ordered Tasks

1. Push this tree to `nissan/reddi-arena` (main).
2. Run `github/create-issues.sh` once; paste issue numbers into spdd/INDEX.md.
3. Create Railway project → deploy from repo → generate domain.
4. Optionally attach a volume and set `DATA_DIR=/data` for persistent
   leaderboard/waitlist.
5. Record the preview URL in README and STATUS.

## N — Norms

- GitHub issues are the project spine; loops anchor on one issue each.
- Artifacts update in the same PR as diverging implementation.
- One-way canonicality; dry-run rail; claims discipline; automation pause
  (see CLAUDE.md Hard Boundaries).

## S — Safeguards / Acceptance Checklist

- [x] No secrets in code, logs, or artifacts (no keypair/RPC/wallet code exists).
- [x] Landing page honesty asserted by tests (no audit/mainnet/production claims).
- [x] Repo pushed; issues created; preview URL recorded.
- [x] Volume decision recorded (ephemeral vs persistent waitlist): **persistent**
      — Railway volume `reddi-arena-data` mounted at `/data`, `DATA_DIR=/data`;
      `/api/health` reports `"persistent": true`.

## Prompt/Code Sync Log

| Date | Divergence | Artifact update | Code/test update |
|---|---|---|---|
| 2026-08-05 | Scaffold + generator built before repo exists | GitHub/Railway tasks pending | 55 tests green locally |
| 2026-08-16 | Preview shipped: main pushed, issues #1–#36 created (GitHub API, no `gh` CLI available), Railway deploy live at https://reddi-arena-production.up.railway.app with persistent `/data` volume | DoD + safeguards checked off; status → done | 55 tests green pre-push; deployed checks pass: /api/health `{"ok": true, "persistent": true}`, `/` 200, `/play` 200, /api/fight (raider vs defender, seed 2) → winner `antweight-vault-raider` |
| 2026-08-16 | O-5 says record URL "in README and STATUS" but the repo has no STATUS.md | Recorded in README only; noted here | — |
